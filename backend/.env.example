# Postgres (recommended)
DATABASE_URL=postgresql+psycopg2://user:pass@localhost:5432/retail_sync

# SQLite fallback (no setup required)
# DATABASE_URL=sqlite:///retail_sync.db

JWT_SECRET=change-me-in-production
